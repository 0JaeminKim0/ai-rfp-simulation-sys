[phases.setup]
nixPkgs = ['nodejs-20_x', 'npm-10_x']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build:railway']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'