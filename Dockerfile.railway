# Railway 배포용 간소화된 Dockerfile
FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 의존성 설치 (--legacy-peer-deps 사용)
RUN npm install --legacy-peer-deps

# 소스 코드 복사
COPY . .

# Railway 환경에서 빌드
RUN RAILWAY=true npm run build

# 포트 노출
EXPOSE ${PORT:-3000}

# 애플리케이션 시작
CMD ["npm", "start"]